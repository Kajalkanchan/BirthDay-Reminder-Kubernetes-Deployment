apiVersion: v1
kind: Service

metadata:
  name: birthday-service

spec:
  type: LoadBalancer

  selector:
    app: birthday-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000